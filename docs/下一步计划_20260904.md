# 下一步计划（2026-09-04）

> 分类原则：**有客观依据、不依赖你的偏好 → 我直接做**；
> 涉及你的处境、习惯或资源 → 留给你拍板。

## 一、不需要讨论，我直接做

判断依据：有实测数据或竞品证据支撑，且不涉及你的个人偏好。

### 1. BC-011 来源可信度（BC-011）
- **依据**：当前写入端零防护；竞品实测 MINJA 仅靠对话即达 98.2% 注入成功率、
  Trojan Hippo 可跨 100 个会话后触发、AgentPoison 用 <0.1% 投毒率即可劫持
- **为什么不用问你**：这是安全底线，不是功能取舍
- **产出**：`source_trust` 字段 + 写入时标注 + 检索可按可信度过滤

### 2. BC-012 写入去重
- **依据**：竞品实测同一事实以不同措辞存 47 次，生产审计 97.8% 是垃圾条目
- **为什么不用问你**：重复存储是明确的浪费，没有"想要重复"的场景
- **产出**：写入前查重，命中则走更新而非新增

### 3. BC-005 supersede 接入
- **依据**：`memory/versioning.py` 已写好但 0/146 用过；BANK600 有 100 道题专门测这个
- **为什么不用问你**：新旧冲突该取新值，没有争议
- **产出**：接入写入路径 + 第一条真实的边（回应"不是图谱"的问题）

### 4. A2 引用校验接线
- **依据**：8月9日跑分暴露 HALLUC_EMPTY（记忆库为空却编出"北邮"），
  而这是生成层问题，检索层修不好——引用校验是唯一能卡住的机制
- **为什么不用问你**：函数已写好，接线是纯技术活
- **产出**：接入 LangGraph 的 verify 节点

### 5. 定时 check + 半天汇总
- **依据**：判定已校准两轮，频率方案你已定（每小时 check、半天汇报、重要立即推）
- **为什么不用问你**：参数你给过了
- **依赖**：判定稳定（已满足）

## 二、需要你拍板（我不替你决定）

### 1. 服务器部署
- 每小时 check 需要常驻进程。你之前有香港/新加坡节点（微知在用）
- **需要你提供**：那台服务器还能不能连（IP / SSH）
- **为什么必须问你**：涉及你的资源和账号，我无法自行获取

### 2. 横向扩哪些信息源
- 日历、本地文件、微知、RSS——**哪个先做取决于你的实际痛点**
- **为什么必须问你**：这是偏好问题，不是技术问题。
  我说"日历重要"毫无意义，只有你知道每天真正烦的是什么

### 3. 推送通道
- 安卓用 Web Push 已确定；但"重要邮件立即推"走哪条路要定
- **为什么必须问你**：打扰阈值是主观的，得你说了算

### 4. 手机端 / 桌面端形态
- 你之前说"没想好"
- **为什么必须问你**：使用习惯，我猜不出来

### 5. 个人主页何时启动
- 已定分家（ADR-012），但没定时间
- **为什么必须问你**：取决于求职紧迫度

## 三、执行顺序

```
现在（不需讨论）
  BC-011 来源可信度 → BC-012 去重 → BC-005 supersede → A2 接线 → 定时 check
        ↓
等你的输入
  服务器信息 → 部署 → 推送 → 横向扩信息源
```

第一条做完，后面每条都依赖它：
去重是 supersede 的前提（先判断"是不是同一件事"才能判断"是不是更新"）；
这两条做完，记忆才第一次有"关系"，判定层第二层（个性化）才有依据。
